# Placeholder test file; this prevents test_dir() from erroring on empty test suite.
test_that("placeholder test passes", {
  expect_true(TRUE)                                    # always-passing test as a sanity check
})
