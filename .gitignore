.Rhistory
.RData
.Rproj.user
.env
.DS_Store
.ipynb_checkpoints/

# Claude Code tooling — local session config and skill cache, not project artefacts
.claude/
.superpowers/

# Shiny runtime logs — generated locally when running shiny::runApp()
shiny_err.log
shiny_out.log
shiny_run.log

# Large model binaries — store locally or in cloud storage, not in git
*.pkl

# NotebookLM PKB documents — local study materials, not repo artefacts
notebooklm/

# Local-dev copies of data files (Dockerfile copies these at build time; not needed in git)
shiny_app/model.csv
shiny_app/selected_cities.csv

# internal planning artifacts
docs/superpowers/

# internal status doc — local reference only, not for public repo
PROJECT-STATUS.md
